//? maybe in the future i will add this to the project

// import { BarChart } from "@mui/x-charts/BarChart";
// const Graph = ({
//     scoreDistribution,
// }: {
//     scoreDistribution: { score: number; amount: number }[];
// }) => {
//     const xAxisData = scoreDistribution.map((item) => item.score);
//     const seriesData = [{ data: scoreDistribution.map((item) => item.amount) }];

//     return (
//         <BarChart
//             className="w-full h-full text-white"
//             colors={["#02a9ff"]}
//             xAxis={[{ scaleType: "band", data: xAxisData }]}
//             series={seriesData}
//             width={450}
//             height={300}
//         />
//     );
// };

// export default Graph;
